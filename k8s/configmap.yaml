apiVersion: v1
kind: ConfigMap
metadata:
  name: chaos-thinker-config
data:
  UPLOAD_DIR: "/app/data/uploads"
  LLAMA_INDEX_DIR: "/app/data/llamaindex"
  LOG_LEVEL: "INFO"
  MAX_UPLOAD_SIZE: "10485760"  # 10MB in bytes
